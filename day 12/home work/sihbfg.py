# sequence is code nwithout repeating and skipping 
# iteration is repeating same code
# selection is selecting the path for the code

# algorithm is the way of telling the computer what to do, like following a recipe

my_name = "Saba"
user_name = input("enter name: ")
if user_name == my_name:
    print(True)
else:
    print(False)

# create variable named "my_name, then create another called "user_name and let the user enter his name,
# then check is my_name and user_name mathc, if yes, then print True, if not, print False